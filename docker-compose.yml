version: "3.8"

services:
  db:
    image: postgres:16
    container_name: torn_rw_feed_db
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - db_data:/var/lib/postgresql/data

volumes:
  db_data:
